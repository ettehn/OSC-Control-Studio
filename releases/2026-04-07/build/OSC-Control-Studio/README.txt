OSC Control Studio local release package.

Run run.cmd to start the desktop host.
Install with installers\Install-OSC-Control-Studio.ps1 from the release folder.
Requires .NET 8 Desktop Runtime and WebView2 Runtime.
